<!-- app/views/profile/update.turbo_stream.erb -->
<%= turbo_stream.replace 'profile_form', partial: 'profile/form', locals: { profile: @profile } %>
<%= turbo_stream.replace dom_id(@profile), partial: 'profile/profile', locals: { profile: @profile } %>
